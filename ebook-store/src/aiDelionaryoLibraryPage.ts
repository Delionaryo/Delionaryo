import './styles.css';
import {createClient} from '@supabase/supabase-js';
const supabase=createClient('https://tordvwlrtwxlbuuzgklt.supabase.co','sb_publishable_s_trbtJvrqcTxDBs_7yyTg_57wHs3sW');
type Cmd={command_id:string;category:string;title:string;description:string;command:string;tags:string[]};
const app=document.querySelector<HTMLDivElement>('#ai-library')!;let commands:Cmd[]=[],active='ALL',term='';
app.innerHTML=`<main class="min-h-screen bg-stone-950 text-stone-100"><header class="sticky top-0 z-20 border-b border-amber-500/20 bg-stone-950/95 backdrop-blur"><div class="mx-auto max-w-7xl px-4 py-4"><div class="flex items-center justify-between gap-3"><div><p class="text-xs font-black tracking-[.22em] text-amber-400">DELIONARYO AI SYSTEM</p><h1 class="text-xl font-black">AI DELIONARYO COMMAND LIBRARY</h1></div><span class="rounded-xl border border-amber-500/30 px-3 py-2 text-xs font-black text-amber-300">STANDALONE SYSTEM</span></div></div></header><div class="mx-auto max-w-7xl px-4 py-8"><section class="overflow-hidden rounded-3xl border border-amber-500/30 bg-gradient-to-br from-stone-900 to-stone-950 p-6 sm:p-8"><p class="text-sm font-black text-amber-400">COMMAND INTELLIGENCE LIBRARY</p><h2 class="mt-2 max-w-3xl text-3xl font-black sm:text-5xl">Find the right AI command. Execute faster.</h2><p class="mt-3 max-w-2xl text-stone-400">A growing DELIONARYO command database for marketing, product research, scripts, images, videos, thumbnails, SEO, content and business systems.</p><div class="mt-6 grid gap-3 sm:grid-cols-[1fr_auto]"><input id="search" class="rounded-2xl border border-stone-700 bg-stone-950 px-5 py-4 outline-none focus:border-amber-400" placeholder="Search command, problem, category or keyword…"><div class="rounded-2xl border border-amber-500/30 px-5 py-4 text-center"><b id="count" class="text-amber-400">0</b><span class="ml-2 text-sm text-stone-400">commands</span></div></div></section><div id="cats" class="mt-5 flex gap-2 overflow-x-auto pb-2"></div><section id="grid" class="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3"><div class="md:col-span-2 xl:col-span-3 p-10 text-center text-stone-500">Loading AI DELIONARYO commands…</div></section></div></main>`;
const esc=(v:string)=>v.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]||m));
function render(){const cats=['ALL',...Array.from(new Set(commands.map(x=>x.category)))];if(!cats.includes(active))active='ALL';document.querySelector('#cats')!.innerHTML=cats.map(c=>`<button data-cat="${esc(c)}" class="whitespace-nowrap rounded-full border px-4 py-2 text-sm font-black ${c===active?'border-amber-400 bg-amber-400 text-stone-950':'border-stone-700 text-stone-300'}">${esc(c)}</button>`).join('');const q=term.toLowerCase(),items=commands.filter(x=>(active==='ALL'||x.category===active)&&(!q||[x.command_id,x.category,x.title,x.description,x.command,...(x.tags||[])].join(' ').toLowerCase().includes(q)));document.querySelector('#count')!.textContent=String(items.length);document.querySelector('#grid')!.innerHTML=items.length?items.map(x=>`<article class="flex flex-col rounded-2xl border border-stone-800 bg-stone-900/40 p-5"><div class="flex items-center justify-between gap-2"><span class="text-xs font-black tracking-widest text-amber-400">${esc(x.command_id)}</span><span class="rounded-full border border-stone-700 px-2.5 py-1 text-xs text-stone-400">${esc(x.category)}</span></div><h3 class="mt-3 text-xl font-black">${esc(x.title)}</h3><p class="mt-2 text-sm text-stone-400">${esc(x.description)}</p><div class="mt-4 flex-1 rounded-xl border border-stone-800 bg-stone-950 p-4 text-sm leading-6 text-stone-300">${esc(x.command)}</div><div class="mt-3 flex flex-wrap gap-2">${(x.tags||[]).map(t=>`<span class="text-xs text-stone-500">#${esc(t)}</span>`).join('')}</div><button data-copy="${esc(x.command_id)}" class="mt-4 w-full rounded-xl bg-amber-400 px-4 py-3 font-black text-stone-950">COPY COMMAND</button></article>`).join(''):`<div class="md:col-span-2 xl:col-span-3 rounded-2xl border border-stone-800 p-10 text-center text-stone-500">No matching command found.</div>`;document.querySelectorAll<HTMLButtonElement>('[data-cat]').forEach(b=>b.onclick=()=>{active=b.dataset.cat!;render()});document.querySelectorAll<HTMLButtonElement>('[data-copy]').forEach(b=>b.onclick=async()=>{const x=commands.find(c=>c.command_id===b.dataset.copy);if(!x)return;await navigator.clipboard.writeText(x.command);const old=b.textContent;b.textContent='COPIED ✓';setTimeout(()=>b.textContent=old,1200)})}
document.querySelector<HTMLInputElement>('#search')!.oninput=e=>{term=(e.target as HTMLInputElement).value;render()};
(async()=>{const{data,error}=await supabase.from('ai_delionaryo_commands').select('command_id,category,title,description,command,tags').eq('is_published',true).order('sort_order').order('command_id');if(error){document.querySelector('#grid')!.innerHTML=`<div class="md:col-span-2 xl:col-span-3 rounded-2xl border border-red-900/50 p-10 text-center text-red-300">Unable to load command library: ${esc(error.message)}</div>`;return}commands=(data||[]) as Cmd[];render()})();